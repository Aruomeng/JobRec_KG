# 深度学习推荐系统

基于 **GraphSAGE** 的职业推荐系统，采用三层漏斗式混合推荐架构。

## 📁 目录结构

```
深度学习推荐/
├── 源代码/
│   ├── 核心模块/           # 核心算法实现
│   │   ├── data_loader.py      # 数据加载与图构建
│   │   ├── model.py            # GraphSAGE模型定义
│   │   ├── train.py            # 模型训练脚本
│   │   ├── hybrid_recommender.py # 三层漏斗混合推荐器
│   │   └── inference_demo.py   # 推理演示
│   │
│   ├── 测试脚本/           # 测试相关
│   │   ├── test_model.py       # 模型效果测试
│   │   ├── test_hybrid.py      # 混合推荐测试
│   │   └── test_loader.py      # 数据加载测试
│   │
│   └── 工具脚本/           # 辅助工具
│       ├── visualize_training.py # 训练可视化
│       ├── run_train.py        # 训练启动器
│       └── debug_run.py        # 调试脚本
│
├── 报告/                   # 所有报告文档
│   ├── 01_GraphSAGE全量数据训练报告_AUC0.9153.md
│   ├── 02_GraphSAGE模型测试报告_10名新学生.md
│   ├── 03_混合推荐系统HybridRecommender测试报告.md
│   └── 可视化图表/         # 训练可视化图表
│       ├── 训练曲线.png
│       ├── 数据规模.png
│       ├── 模型参数.png
│       ├── 专业分布.png
│       └── 训练仪表盘.png
│
├── 数据/                   # 数据文件
│   ├── graph_data.pt           # PyG图数据 (~1GB)
│   └── 模型权重/
│       └── graphsage_model.pth # 训练好的模型
│
├── 日志/                   # 训练日志
│   ├── train.log
│   └── train_full.log
│
└── README.md               # 本文件
```

## 🚀 快速开始

### 1. 训练模型

```bash
cd 源代码/核心模块
python3 train.py
```

### 2. 测试推荐

```bash
cd 源代码/核心模块
python3 inference_demo.py
```

### 3. 使用混合推荐器

```python
from hybrid_recommender import create_recommender_from_trained_model

recommender = create_recommender_from_trained_model()
results = recommender.recommend('STU0001', final_k=5)
```

## 📊 模型性能

| 指标     | 数值                   |
| -------- | ---------------------- |
| 训练数据 | 31,487 职位 / 500 学生 |
| 训练样本 | 892,760 条             |
| 最终 AUC | **0.9153**             |
| 推荐延迟 | < 500ms                |

## 🏗️ 架构

```
Layer 1 (召回) → Layer 2 (精排) → Layer 3 (融合)
   ↓                ↓               ↓
向量相似度       深度学习模型      Neo4j + 规则
  Top-500         排序评分         可解释推荐
```

---

**最后更新**: 2026-01-13
